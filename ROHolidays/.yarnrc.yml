nodeLinker: node-modules
enableGlobalCache: true
npmRegistryServer: "https://registry.npmjs.org"
# We stick to the node-modules linker because React Native tooling and Metro
# expect a classic node_modules layout; PnP still has edge cases with native
# modules and watchman.
